<%= f.error_messages %>
<p>
  <label for="stu">姓名:</label>
  <label for="name"><%=h @step1.student.name %></label>
</p>
<p>
  <label for="dep">专业:</label>
  <label for="name">
    <%=h @step1.student.department.name if @step1.student.department %>
    <%=h @step1.student.major.name if @step1.student.major %>
  </label>
</p>
<p>
  <label for="info_cla">班级:</label>
  <label for="name"><%=h @step1.student.info_class.name if @step1.student.info_class %></label>
</p>
<p>
  <label>复查:</label>
  <label for="check"><%= f.radio_button :step1, true  %>通过</label>
  <label for="check"><%= f.radio_button :step1, false %>未通过</label>
</p>