<fieldset>
  <legend>查看[<%= @student.name  %>的档案</legend>
  <p>
    <b>姓名:</b>
    <%=h @student.name %>
  </p>
  <p class="bbar">
    <input type="button" onclick="$page.showListView()" value="返回列表" />
  </p>
</fieldset>